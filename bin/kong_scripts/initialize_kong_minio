# Set up the Upstream for the MinIO Object Server
curl -X POST http://localhost:8001/upstreams/ \
--data "name=minio";

# Now, add Targets to the Upstream. These will be just the one MinIO server we have running
curl -X POST http://localhost:8001/upstreams/minio/targets \
--data "target=localhost:9000" \
--data "weight=100";

# Next, create a Service targeting the Upstream
curl -X POST http://localhost:8001/services/ \
--data "name=minio_service" \
--data "host=minio" \
--data "path=/";

# Finally, we'll add a Route as an entry-point into the Service
curl -X POST http://localhost:8001/services/minio_service/routes/ \
--data "paths[]=/minio";




